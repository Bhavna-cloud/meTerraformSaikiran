#Task3 -- Dynamodb locking

```

terraform apply --auto-approve
git init
git remote
git remote add origin https://github.com/Bhavna-cloud/me_dynamo_saikiran_terraform. gitremote -v
git status
git add .
git commit -m "added daily files of terraform"
git status
git branch
git push origin master

```
